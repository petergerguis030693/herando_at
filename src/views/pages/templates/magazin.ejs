  <!-- Header -->
  <%- include('../../partials/header', { entieties, active: 'magazin' }) %>
  <link rel="stylesheet" href="/css/magazin.min.css">

  <div class="container my-4">
    <!-- Breadcrumb -->
    <nav aria-label="breadcrumb" class="mb-4">
      <ol class="breadcrumb bg-transparent p-0 mb-0">
        <li class="breadcrumb-item"><a href="/" class="text-decoration-none"><%= t('magazin.home', 'Home') %></a></li>
        <li class="breadcrumb-item active" aria-current="page"><%= t('magazin.breadcrumb', 'Magazin') %></li>
      </ol>
    </nav>


    <% if (magazinPosts.length) { 
         const featured = magazinPosts[0];
         const others   = magazinPosts.slice(1);
    %>

      <!-- Featured Article -->
      <section class="featured">
        <a href="/magazin/<%= featured.slug %>">
          <img src="<%= featured.image %>" alt="<%= featured.title %>" width="1200" height="675" loading="lazy" decoding="async">
        </a>
        <div class="featured-content">
          <% if (featured.published_at && new Date(featured.published_at) > new Date()) { %>
            <p class="small text-muted mb-1"><%= t('magazin.badge_upcoming', 'Demnächst') %> · <%= new Date(featured.published_at).toLocaleString('de-DE', { dateStyle: 'medium', timeStyle: 'short' }) %></p>
          <% } %>
          <h1>
            <a href="/magazin/<%= featured.slug %>" class="text-decoration-none text-dark">
              <%= featured.title %>
            </a>
          </h1>
          <p><%= featured.excerpt %></p>
        </div>
      </section>

      <!-- Aktuelle Artikel -->
      <section>
        <h2 class="mb-3">
          <%= t('magazin.latest_articles', 'Aktuelle Artikel') %>
        </h2>
        <div class="mag-grid">
          <% others.slice(0,4).forEach(post => { %>
            <div class="mag-item shadow-sm">
              <a href="/magazin/<%= post.slug %>">
                <img src="<%= post.image %>" alt="<%= post.title %>" width="600" height="338" loading="lazy" decoding="async">
              </a>
              <div class="info">
                <% if (post.published_at && new Date(post.published_at) > new Date()) { %>
                  <p class="small text-muted mb-1"><%= t('magazin.badge_upcoming', 'Demnächst') %> · <%= new Date(post.published_at).toLocaleString('de-DE', { dateStyle: 'medium', timeStyle: 'short' }) %></p>
                <% } %>
                <h3>
                  <a href="/magazin/<%= post.slug %>" class="text-decoration-none text-dark">
                    <%= post.title %>
                  </a>
                </h3>
                <p><%= post.excerpt %></p>
              </div>
            </div>
          <% }) %>
        </div>
      </section>

      <!-- Weitere Artikel -->
      <% if (others.length > 4) { %>
        <section id="more-articles" style="display:none;">
          <div class="mag-grid">
            <% others.slice(4).forEach(post => { %>
              <div class="mag-item shadow-sm">
                <a href="/magazin/<%= post.slug %>">
                  <img src="<%= post.image %>" alt="<%= post.title %>" width="600" height="338" loading="lazy" decoding="async">
                </a>
                <div class="info">
                  <% if (post.published_at && new Date(post.published_at) > new Date()) { %>
                    <p class="small text-muted mb-1"><%= t('magazin.badge_upcoming', 'Demnächst') %> · <%= new Date(post.published_at).toLocaleString('de-DE', { dateStyle: 'medium', timeStyle: 'short' }) %></p>
                  <% } %>
                  <h3>
                    <a href="/magazin/<%= post.slug %>" class="text-decoration-none text-dark">
                      <%= post.title %>
                    </a>
                  </h3>
                  <p><%= post.excerpt %></p>
                </div>
              </div>
            <% }) %>
          </div>
        </section>

        <!-- Load More Button -->
        <div class="load-more-wrapper">
          <button id="load-more" class="load-more-btn">
            <%= t('magazin.load_more', 'Mehr anzeigen') %>
          </button>
        </div>
      <% } %>
    <% } %>

    <!-- JS: "Mehr anzeigen" Funktion -->
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const btn = document.getElementById('load-more');
        const more = document.getElementById('more-articles');
        if (btn && more) {
          btn.addEventListener('click', () => {
            more.style.display = 'block';
            btn.style.display = 'none';
          });
        }
      });
    </script>
  </div>

  <%- include('../../partials/footer', { footerColumns }) %>
</body>
</html>
