<div class="product-single-images">
  <div class="img-magnifier-container">
    <img id="mainImage"
         src="<%= item.imageUrl %>"
         alt="<%= item.title %>" />
    <div class="img-magnifier-glass"></div>
  </div>
</div>
